apiVersion: v1
kind: Service
metadata:
  name: game-engine-wiki-mcp
spec:
  type: NodePort
  selector:
    app: game-engine-wiki-mcp
  ports:
    - name: http
      port: {{ .Values.mcp.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
