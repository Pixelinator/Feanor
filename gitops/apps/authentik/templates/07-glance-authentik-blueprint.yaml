apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints-glance
  namespace: authentik
data:
  # NOTE: provider/application only. The embedded-outpost assignment lives
  # centrally in authentik-blueprints-base (90-outposts.yaml) because
  # blueprints SET the outpost provider list -- per-app entries would
  # overwrite each other.
  glance.yaml: |
    version: 1
    metadata:
      name: homelab-glance
    entries:
      - model: authentik_providers_proxy.proxyprovider
        state: present
        identifiers:
          name: Provider for glance
        attrs:
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          mode: forward_single
          external_host: https://glance.eldamar.dev

      - model: authentik_core.application
        state: present
        identifiers:
          slug: glance
        attrs:
          name: glance
          group: home
          provider: !Find [authentik_providers_proxy.proxyprovider, [name, Provider for glance]]
          policy_engine_mode: any
